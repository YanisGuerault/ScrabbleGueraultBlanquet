<resources>

    <style name="ThemeParent" parent="Theme.AppCompat.Light">
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
        <item name="editTextBackground">@color/white</item>
    </style>

    <style name="ThemeClassic" parent="ThemeParent">
        <item name="android:colorBackground">@color/green</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:titleTextColor">@color/black</item>
        <item name="android:editTextColor">@color/black</item>
    </style>

    <style name="ThemeSombre" parent="ThemeParent">
        <item name="android:colorBackground">@color/darkblue</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:titleTextColor">@color/white</item>
        <item name="android:editTextColor">@color/white</item>
    </style>

    <style name="ThemeAutre" parent="ThemeParent">
        <item name="android:colorBackground">@color/blue</item>
        <item name="android:textColor">@color/pink</item>
        <item name="android:titleTextColor">@color/pink</item>
        <item name="android:editTextColor">@color/pink</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
